services:
  nandepoly:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ROOM_TTL_HOURS=12
    volumes:
      - nandepoly_data:/data
    restart: unless-stopped
volumes:
  nandepoly_data:
